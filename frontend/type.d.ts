interface Category {
    id: number;
    image: string;
    name: string;
}
interface CategoryProps {
    category: Category;
}

interface Promo {
    id: number;
    image: string;
}
interface PromoProps {
    promo: Promo;
}