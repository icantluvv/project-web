import React from "react";

const page = () => {
    return (
        <div className="min-h-[35em] w-full flex justify-center items-center">
            All categories page
        </div>
    );
};

export default page;
